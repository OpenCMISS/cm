<chapter>
  <title>Object Interface</title>
  <section>
    <title>Objectives</title>
    <itemizedlist>
      <listitem>Simple interface for a variety of scripting and programming languages</listitem>
      <listitem>User friendly in that the library should sensibly set default parameters to minimise the amount of information the programmer/user has to send</listitem>
      <listitem>Should be extensible so that extra parameters can be added at a later stage without causing problems</listitem>
    </itemizedlist>
  </section>
  <section>
    <title>General Rules</title>
    <itemizedlist>
      <listitem>Objects identified by a unique user number</listitem>
      <listitem>Objects are initialised with a <code>OBJECT_INITIALISE</code> call</listitem>
      <listitem>Objects are finalised with a <code>OBJECT_FINALISE</code> call</listitem>
      <listitem>Objects are created with a pair of <code>OBJECT_CREATE_START</code> and <code>OBJECT_CREATE_FINISH</code> calls</listitem>
      <listitem>Objects are destroyed with a <code>OBJECT_DESTROY</code> call</listitem>
      <listitem>Object parameters are set with a number of <code>OBJECT_PARAMETER_SET</code> calls between the START and FINISH calls</listitem>
      <listitem>START call initialises OBJECT and sets default parameters. SET calls modify default parameters. FINISH call finalises the object</listitem>
    </itemizedlist>
  </section>
  <section>
    <title>Example(Basis functions)</title>
    &basis_call;
  </section>
</chapter>
